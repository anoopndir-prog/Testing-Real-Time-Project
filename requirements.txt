# Pinned to exact versions used for the tested/released build, so every
# rebuild (dev or the Windows .exe release) uses identical dependency code.
# Bump deliberately and re-test, don't widen back to '>=' ranges.
openpyxl==3.1.5
python-docx==1.2.0
Pillow==12.2.0
docx2pdf==0.1.8
# Not verified from this (macOS) environment - pin to the exact version
# confirmed working after the first real Windows build/test, then update.
pywin32>=311; platform_system == "Windows"
tkinterdnd2==0.4.3
tkcalendar==1.6.1
