# Pinned to exact versions used for the tested/released build, so every
# rebuild (dev or the Windows .exe release) uses identical dependency code.
# Bump deliberately and re-test, don't widen back to '>=' ranges.
openpyxl==3.1.5
python-docx==1.2.0
pypdf==6.4.0
# 12.3.0 is a security floor, not just a version bump: 12.2.0 carries 20
# known CVEs in the image parsers, and this app opens user-supplied photo
# files (app/seal_photo.py), which is exactly that attack surface. Do not
# pin back below 12.3.0.
Pillow==12.3.0
# Seal identification photo auto-crop (app/seal_photo.py): background-
# subtraction heuristic over a numpy array, no heavier CV/ML dependency.
numpy==2.3.4
docx2pdf==0.1.8
# OCR fallback for scanned/handwritten monitoring sheets that have no
# embedded PDF text layer. PyMuPDF/pytesseract are pip packages, but
# pytesseract also needs the Tesseract OCR binary installed on the
# machine separately (pip cannot install it): `brew install tesseract`
# on macOS, or the Windows installer from the Tesseract project, added
# to PATH before running/building the app.
PyMuPDF==1.28.0
pytesseract==0.3.13
# Not verified from this (macOS) environment - pin to the exact version
# confirmed working after the first real Windows build/test, then update.
pywin32>=311; platform_system == "Windows"
tkinterdnd2==0.4.3
tkcalendar==1.6.1
# Browser (HTML) tool: app/web_app.py. waitress is the production WSGI
# server it's always run under (never Flask's own dev server) - pure
# Python, no C-extension build step, bundles cleanly with PyInstaller.
Flask==3.1.3
waitress==3.0.2
